# This is an Environment Variable (or dot env) file
# It is used to set environment variables for FUME Community server runtime configuration

# To use this example file, copy it to .env in the same directory as this file.

# FUME server port
#SERVER_PORT=42420

# Max request body size accepted by the HTTP server (used for json/xml/csv/hl7v2)
# Examples: 10mb, 100kb, 1gb
#SERVER_REQUEST_BODY_LIMIT=400mb

# FHIR server API endpoint
# Set to 'n/a' to disable the server source
#FHIR_SERVER_BASE=http://123.123.123.123/fhir

# FHIR Server auth details
#FHIR_SERVER_AUTH_TYPE=BASIC
#FHIR_SERVER_UN=username
#FHIR_SERVER_PW=password

# FHIR server timeout in milliseconds
#FHIR_SERVER_TIMEOUT=5000

# Default FHIR version
#FHIR_VERSION=4.0.1

# FHIR packages to load (one package@version or a comma separated list of package@version)
# If you want the latest version of a package to be downloaded at startup, just omit the @version part
#FHIR_PACKAGES=il.core.fhir.r4@0.11.0,hl7.fhir.us.core

# Optional: Custom FHIR package registry for enterprise environments
# Example: JFrog Artifactory, Nexus, Azure DevOps Artifacts
# If not set, defaults to public registries (packages.fhir.org, packages.simplifier.net)
#FHIR_PACKAGE_REGISTRY_URL=https://your-artifactory.example.com/artifactory/api/npm/fhir-npm-remote

# Optional: Authentication token for private registry (base64-encoded, no "Bearer" prefix)
# Only used when FHIR_PACKAGE_REGISTRY_URL is set
#FHIR_PACKAGE_REGISTRY_TOKEN=your-token-here

# Optional: Local cache directory for downloaded FHIR packages
# If set, packages will be stored/reused here instead of the default location
# Example (Windows): C:\fhir-cache  |  Example (Linux/macOS): /var/tmp/fhir-cache
#FHIR_PACKAGE_CACHE_DIR=C:\fhir-cache

# Optional: Mappings folder for file-based mappings and aliases.json
# Set to 'n/a' or leave empty to disable file mappings
#MAPPINGS_FOLDER=./mappings

# Optional: File extension for mappings (default is .fume)
#MAPPINGS_FILE_EXTENSION=.fume

# Optional: File polling interval in milliseconds (set <= 0 to disable)
#MAPPINGS_FILE_POLLING_INTERVAL_MS=5000

# Optional: Server polling interval in milliseconds (set <= 0 to disable)
#MAPPINGS_SERVER_POLLING_INTERVAL_MS=30000

# Optional: Forced resync interval in milliseconds (set <= 0 to disable)
#MAPPINGS_FORCED_RESYNC_INTERVAL_MS=3600000

# Optional: Internal compiled-expression cache size (entries)
#FUME_COMPILED_EXPR_CACHE_MAX_ENTRIES=1000

